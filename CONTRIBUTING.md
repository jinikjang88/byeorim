# 기여 안내 (Contributing)

벼림에 관심을 가져줘서 고맙다. 이 문서는 외부 기여자가 어디서 시작해서 어디로 PR을 보낼지를 안내한다. 작은 오타 수정부터 새 카탈로그 추가까지 모두 환영한다.

## 시작하기 전에 두 글을 읽는다

벼림은 코드 더미가 아니라 약속이다. 어떤 약속인지를 두 문서가 설명한다.

- [MANIFESTO](docs/MANIFESTO.md): 벼림이 왜 태어났는가, 누구를 위한 것인가
- [CLAUDE.md](CLAUDE.md): 코드와 문서를 만질 때 따라야 하는 약속

CLAUDE.md는 이름 그대로 Claude Code와 작업할 때 읽으라고 만든 문서지만, 그 안의 약속(절대 금지선, 테스트 정책, 워크로그/ADR 요구, 글쓰기 원칙)은 사람 기여자에게도 그대로 적용된다. 다른 AI 도구를 쓰든 IDE에서 직접 코드를 짜든 같다.

이 두 글을 한 번 읽고 와야 PR이 거절되는 일이 줄어든다. 길지 않다.

## 개발 환경

```bash
git clone https://github.com/jinikjang88/byeorim.git
cd byeorim
npm install
npm test
```

- Node.js 18 이상 필요
- 모든 테스트가 통과해야 시작점이 안전하다
- 테스트가 깨진 채로 main에 들어오는 일은 없으니, 깨졌다면 환경이나 설치를 의심한다

자주 쓰는 명령은 다음과 같다.

| 명령 | 무엇을 하는가 |
|------|--------------|
| `npm test` | 단위 테스트와 통합 테스트 실행 |
| `npm run test:e2e` | 엔드투엔드 테스트 실행 (시간이 더 걸린다) |
| `npm run lint` | ESLint 검사 |
| `npm run format` | Prettier로 코드 정렬 |
| `npm run format:check` | 포맷 위반만 보고 (CI에서 쓰는 방식) |

## 무엇을 기여할 수 있나

기여 결을 크게 셋으로 나눈다.

### 버그 신고와 수정

재현되는 버그를 찾았다면 [GitHub Issues](https://github.com/jinikjang88/byeorim/issues)에 올린다. 다음을 포함하면 빠르게 다룰 수 있다.

- 어떤 명령을 실행했는가
- 어떤 결과를 기대했는가
- 어떤 결과가 나왔는가
- Node.js 버전과 OS

수정 PR을 같이 보내고 싶다면 환영한다. 다만 PR 본문에 어떤 이슈를 닫는지 적어둔다.

### 기능 제안

새 명령, 새 의존성 타입, 새 검증 결을 제안한다면 먼저 이슈로 토론한다. 코드부터 보내면 거절될 수 있다. 벼림의 1번 약속이 "설계 없이 코드를 쓰지 않는다"라서, 외부 기여도 같은 결을 따른다.

이슈에는 다음을 적는다.

- 어떤 사용자(1순위 비기술 창업자, 2순위 신사업 개발자, 3순위 시니어 개발자)에게 어떤 문제가 있는가
- 어떤 결로 풀려고 하는가
- 7단계 메타포(prospect/smelt/shape/forge/temper/set/inspect) 중 어디에 속하는가
- ADR이 필요한 결정인가 (CLAUDE.md 섹션 6 참고)

메인테이너와 합의가 되면 그때 코드를 짠다.

### 카탈로그 진화

`packages/templates/` 아래 도메인 카탈로그(예: commerce, reservation)를 늘리거나 다듬는 기여도 환영한다. 카탈로그는 코드가 아니라 데이터라 ADR이 거의 필요 없지만, 의존성 타입(requires/affects)을 새로 추가하는 결이라면 ADR이 필요하다.

새 도메인 카탈로그를 추가한다면 `docs/catalog-evolution/`에 진화 노트를 함께 남긴다. 왜 이 블럭을 넣었고 왜 이 의존성을 박았는지 6개월 뒤 사람이 추적할 수 있어야 한다.

### 문서 다듬기

오타 수정, 문장 다듬기, 예시 추가는 이슈 없이 바로 PR을 보내도 된다. 다만 [CLAUDE.md 섹션 10](CLAUDE.md)의 글쓰기 원칙은 따른다. 본문 안 별표 강조, em dash, 화살표 도식, 광고 카피 같은 마무리는 피한다.

## PR 흐름

1. 작업할 이슈를 정한다 (오타 수정 같은 작은 결은 이슈 없이 바로 가능)
2. 포크하고 새 브랜치를 판다. 브랜치 이름은 자유롭지만 의도가 보이게 짓는다 (예: `fix-smelt-import-edge-case`, `add-reservation-template`)
3. 코드를 짠다. 같은 PR에 테스트도 함께 짠다 ("나중에 테스트"는 없다)
4. 변경 사항이 아키텍처를 흔든다면 `docs/decisions/NNNN-슬러그.md`로 ADR을 작성한다. 무엇이 ADR이 필요한지는 [CLAUDE.md 섹션 6](CLAUDE.md) 참고
5. `docs/worklog/YYYY-MM-DD.md`에 워크로그 항목을 남긴다. 템플릿은 [CLAUDE.md 섹션 5](CLAUDE.md) 참고
6. PR을 연다. 본문에 어떤 이슈를 닫는지, 무엇이 바뀌었고 무엇이 남아있는지 적는다

## PR이 머지되려면

다음이 모두 참이어야 한다.

- [ ] `npm run lint` 통과
- [ ] `npm run format:check` 통과
- [ ] `npm test` 통과
- [ ] 새 코드에 테스트가 함께 있다 (단위 테스트 + 해당하면 통합 테스트)
- [ ] 커버리지가 떨어지지 않는다 (core/catalog 90%, cli/ai 70% 이상)
- [ ] ADR이 필요한 결정이라면 ADR이 함께 있다
- [ ] 워크로그 항목이 있다
- [ ] [CLAUDE.md 섹션 2](CLAUDE.md)의 절대 금지선을 어기지 않았다
- [ ] CLI 출력이 비기술 사용자도 읽을 수 있는 한국어다 (영문 스택 트레이스를 그대로 노출하지 않는다)

검토는 메인테이너가 직접 한다. 답이 며칠 걸릴 수 있다. 1인 메인테이너가 운영하는 프로젝트라는 점을 양해해 준다.

## 코드 스타일

자세한 결은 [CLAUDE.md 섹션 8](CLAUDE.md)에 있다. 외부 기여자가 자주 부딪히는 부분만 짚는다.

- ESM 모듈만 쓴다. CommonJS는 쓰지 않는다
- 도메인 함수 이름은 7단계 메타포에서 가져온다 (`extract()`보다 `smelt()`가 옳다)
- 같은 패키지 안의 기존 함수와 같은 패턴으로 짠다 (인자 순서, 반환 형태, 에러 처리)
- 한 파일에는 가능한 한 한 기능만 둔다
- 카탈로그의 특정 ID(예: commerce의 `coupon`, `payment`)를 코드 본문에 박지 않는다. 1순위 사용자가 commerce가 아닌 도메인 사람이라는 사실을 코드도 지킨다
- 주석은 한국어를 환영한다. 도메인 용어와 결이 잘 붙는다

## 첫 기여자에게

`good first issue` 라벨이 붙은 이슈가 시작점이다. 작은 결로 흐름을 한 번 익히면 다음 PR이 훨씬 가볍다.

질문은 부끄러운 것이 아니다. 정직한 것이다. 막히는 부분이 있으면 이슈에 댓글로 묻거나 새 이슈를 연다. "이 결이 맞나요?"라는 질문이 잘못된 가정으로 코드를 커밋하는 것보다 훨씬 낫다. CLAUDE.md 섹션 7도 같은 결을 말한다.

## 행동 강령

이 저장소의 모든 공간은 [행동 강령](CODE_OF_CONDUCT.md)을 따른다. 코드를 짤 줄 모르는 사람과 10년차 개발자가 같은 이슈 스레드에서 만났을 때 둘 다 안심하고 질문할 수 있어야 한다.

## 라이선스

벼림은 [MIT 라이선스](LICENSE)다. 기여한 코드는 같은 라이선스로 공개된다.
