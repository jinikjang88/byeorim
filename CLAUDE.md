# CLAUDE.md — Byeorim 작업 지침

이 문서는 Claude Code와 Byeorim 저장소 사이의 약속이다. 모든 세션 시작 시 읽는다. 요약하지 않고 그대로 따른다. 이 문서와 어긋나는 일을 하려는 순간이 오면 멈추고 먼저 묻는다.

이 문서가 항구적 지침의 단일 원천이다. 별도 메모리 시스템(자동 메모리, 메모 파일, 외부 노트)에 지침을 흩뿌리지 않는다. 사용자가 새 규칙이나 선호를 말하면 메모리에 저장하지 말고 이 문서의 알맞은 절에 추가한다. 다음 세션에서도 이 문서만 참고한다.

---

## 0. 가장 먼저 (READ FIRST)

코드 한 줄 쓰기 전에 docs/MANIFESTO.md를 읽는다. 이 문서 아래 항목은 작업 메커니즘이고, MANIFESTO는 왜 이 일을 하는지 답한다. 너의 제안이 MANIFESTO를 섬기지 않는다면 그것은 잘못된 제안이다. 기술적으로 아무리 영리해 보여도 마찬가지다.

### Byeorim의 한 줄 사명
AI를 활용해 자신의 서비스를 만들고 싶은 사람을 돕는다. 막연한 아이디어를 코드 이전에 볼 수 있는 블럭으로 빚고, 그 시장에서 어떤 질문을 안고 가야 할지를 함께 본다. 답을 강요하지 않고 답하지 못한 질문은 다이어리에 남겨두며 동행한다.

### 우리가 섬기는 사용자 (우선순위 순)
1. **비기술 창업자**: 아이디어와 자금은 있지만 코딩은 못 하는 카페 사장님, 학원 원장님, 도메인 전문가들
2. **신사업 개발자**: 새 도메인을 탐색하면서 아키텍처를 확정하기 전에 체계적인 발판이 필요한 엔지니어
3. **시니어 개발자**: CLI와 YAML을 직접 다루고 싶은 파워 유저

3번 사용자만 도와주고 1번과 2번을 혼란스럽게 만드는 기능은 퇴보다.

---

## 1. 프로젝트 성격 (PROJECT NATURE)

- **라이선스**: MIT. 무료 오픈소스 프로토타입이자 포트폴리오 프로젝트
- **수익화 없음**: 유료 등급, 게이트 기능, 텔레메트리, "Pro" 어쩌고를 제안하지 않는다. 차후 상용화 결정은 명시적 ADR로 한다
- **품질 기준**: 프로토타입이지만 프로덕션급 코드 품질을 유지한다. 포트폴리오 가치는 기능 개수가 아니라 장인정신에서 나온다
- **정체성**: 한국에서 시작된 글로벌 오픈소스 표준이 비전이다. 프로젝트명은 한국어, 작업 어휘는 영문으로 이중화한다

---

## 2. 절대 금지선 (RED LINES — NEVER CROSS)

이 일곱 가지는 어떤 상황에서도 어기지 않는다. "조금 빨리 가려고", "이번 한 번만", "나중에 고치면 되니까" 같은 핑계가 떠오르면 그 순간이 멈춰야 할 때다.

### 1) 설계 없이 코드를 쓰지 않는다 (NO CODE BEFORE DESIGN)
.byeorim/project/ 안에 intent.yml, selected-blocks.yml, contracts.yml 중 하나라도 없으면 애플리케이션 코드를 생성하지 않는다. 사용자가 "그냥 빨리 코드부터 보여줘"라고 해도 거절한다. 이걸 어기는 순간 Byeorim은 또 하나의 Cursor가 된다. Byeorim이 태어난 이유 자체가 무너진다.

### 2) 답을 강요하지 않는다 (NEVER FORCE AN ANSWER)
사용자가 답할 수 없는 질문을 만났을 때 다음 단계로 못 가게 막지 않는다. 답하지 못한 질문은 다이어리(.byeorim/project/diary.md)에 남겨두고 함께 다음 단계로 간다. 강한 신호를 줘야 할 때는 출시 직전 법적 리스크 같이 진짜 멈춰야 할 때뿐이다. 평소 톤은 브레이크가 아니라 동행이다.

### 3) 테스트 없는 코드를 머지하지 않는다 (NO CODE WITHOUT TESTS)
새 모듈은 같은 PR에서 테스트와 함께 출시된다. "급하니까 테스트는 다음에"는 없다. 테스트 없이 머지된 코드는 Byeorim의 신뢰를 갉아먹는다. 자세한 정책은 섹션 4 참조.

### 4) 다이어리와 ADR을 빠뜨리지 않는다 (NEVER SKIP THE HISTORY)
의미 있는 작업 세션마다 워크로그(docs/worklog/YYYY-MM-DD.md)를 남긴다. 아키텍처 결정을 내렸으면 ADR(docs/decisions/NNNN-슬러그.md)을 쓴다. 역사 없는 코드 더미는 6개월 뒤 아무도 손대지 못한다. 자세한 정책은 섹션 5, 섹션 6 참조.

### 5) YAML을 사용자에게 들이밀지 않는다 (YAML IS NEVER THE UI)
사용자는 .byeorim/ 안의 YAML을 직접 만지지 않는다. YAML은 AI와 코드 생성기 사이의 중간 언어다. 단 YAML 자체는 사람이 읽을 수 있고 diff가 깨끗해야 한다. 머신용이라고 해서 난독화하지 않는다.

### 6) 특정 AI 벤더에 종속시키지 않는다 (NEVER LOCK-IN A VENDOR)
.byeorim/ 디렉토리는 Claude로도, GPT로도, Gemini로도, 미래의 어떤 모델로도 읽을 수 있어야 한다. Anthropic API 전용 호출 패턴이나 Claude 특정 프롬프트 형식을 코어에 박지 않는다. 모델 어댑터는 packages/ai/ 안에서만 산다.

### 7) 유료 기능, 텔레메트리, 종속 코드를 슬쩍 넣지 않는다 (NO PAID, NO TELEMETRY, NO TRACKING)
Byeorim은 무료 오픈소스 프로토타입이다. "Pro", "Premium", "Enterprise" 같은 게이트 코드를 PR에 끼워 넣지 않는다. 사용자 행동을 수집하는 텔레메트리도 명시적 옵트인 없이는 추가하지 않는다. 차후 상용화가 필요해지면 그때 ADR로 명시적으로 결정한다. 슬쩍 들어가는 일은 없다.

이 일곱 줄은 Byeorim의 약속이다. 이걸 어기는 PR은 머지되지 않는다.

---

## 3. 멀티 모듈 구조 (MULTI-MODULE ARCHITECTURE)

Byeorim은 모듈 경계가 엄격한 모노레포다.

```
byeorim/
├── packages/
│   ├── core/         # 순수 도메인: World, Bundle, Block, Dependency, CascadeRule
│   │                 # I/O 없음, CLI 의존성 없음, 타입과 순수 함수만
│   ├── catalog/      # 카탈로그 로더, 검증기, 의존성 해결기
│   │                 # 의존: core
│   ├── cli/          # 사용자 대면 CLI (byeorim prospect/smelt/shape/forge/temper/inspect)
│   │                 # 의존: core, catalog, ai
│   ├── ai/           # 모델 무관 AI 어댑터 (Claude/GPT/Gemini)
│   │                 # 의존: core
│   └── templates/    # 도메인 카탈로그 (commerce, reservation 등)
│                     # 데이터만, 코드 없음
├── docs/
│   ├── MANIFESTO.md
│   ├── decisions/    # ADR
│   ├── worklog/      # 일일 워크로그
│   └── catalog-evolution/  # 블럭/의존성 변경 노트
├── .byeorim/         # 자체 적용: Byeorim이 Byeorim을 Byeorim으로 설계
└── tests/            # 패키지 간 통합 테스트와 E2E
```

### 의존성 규칙 (엄격하게 강제)

```
templates  → 데이터, 의존성 없음
core       → 내부 의존성 없음
catalog    → core
ai         → core
cli        → core, catalog, ai
tests      → 전부
```

하위 레이어가 상위 레이어를 import하면 안 된다. core가 cli를 아는 것은 버그다. 린트 규칙으로 강제한다. 사람의 규율에만 의존하지 않는다.

### 새 기능 추가 시 자문 순서
1. core(순수 도메인 로직)에 속하는가? 그렇다면 거기에 둔다
2. 파일시스템이나 I/O가 필요한가? catalog 또는 cli에 둔다
3. LLM 호출이 필요한가? ai 패키지 안 인터페이스 뒤에 둔다. core와 catalog는 그것을 mock할 수 있어야 한다
4. 코드가 아니라 데이터인가? templates에 둔다

답이 "전부에 걸친다"라면 설계가 잘못된 것이다. 분리한다.

---

## 4. 테스트와 검증 (TESTING — REQUIRED, NOT OPTIONAL)

### 레이어
- **단위 테스트** (패키지별): 순수 로직, I/O 없음, 빠를 것(전체 5초 미만)
  - core: 의존성 해결, cascade 규칙, 결정 수집
  - catalog: YAML 스키마 검증, 로더 엣지 케이스
  - ai: 프롬프트 조립, 응답 파싱(LLM은 mock)
- **통합 테스트** (tests/integration/): 패키지들이 함께 동작. 실제 파일시스템, mock된 LLM
- **E2E 테스트** (tests/e2e/): 임시 디렉토리에서 CLI를 끝까지 실행. `byeorim init` → `byeorim smelt` → 생성된 .byeorim/이 유효한지 단언

### 테스트 작성 패턴
주력은 Given-When-Then이다. 다른 패턴도 상황에 맞게 사용한다.

**Given-When-Then (BDD 스타일, 주력)**: 기능 단위 테스트의 기본 패턴. 의도가 명확하게 드러나서 비개발자도 읽을 수 있다. 핵심 도메인 로직과 통합 테스트에는 이 패턴을 쓴다.

```js
describe('resolveAll', () => {
  it('쿠폰 의존성을 결제, 환불, 정산으로 cascade한다', () => {
    // Given: 쿠폰, 결제, 환불, 정산 블럭이 있는 카탈로그
    const catalog = loadFixture('commerce-minimal');
    // When: 사용자가 쿠폰 블럭만 선택
    const result = resolveAll(['coupon'], catalog);
    // Then: 결제, 환불, 정산이 type='affects'로 추가됨
    expect(result.affected).toEqual(
      expect.arrayContaining(['payment', 'refund', 'settlement'])
    );
  });
});
```

**Property-based testing (속성 기반 검증)**: 입력 범위가 넓거나 cascade 규칙처럼 조합이 폭발하는 경우. fast-check 같은 라이브러리로 무작위 입력을 던져 불변식을 검증한다. 의존성 해결기, 카탈로그 검증기에 적합하다.

```js
import fc from 'fast-check';

it('어떤 블럭 조합도 의존성 해결 후 순환이 없다', () => {
  fc.assert(fc.property(fc.array(fc.constantFrom(...allBlockIds)), (selected) => {
    const result = resolveAll(selected, catalog);
    expect(hasCycle(result.required)).toBe(false);
  }));
});
```

**Snapshot testing (스냅샷 검증)**: AI가 생성한 출력의 구조가 시간에 따라 망가지지 않는지 본다. Prospect의 카탈로그 출력, Reality Check 리포트 형식, 생성된 .byeorim/ 디렉토리 구조에 적합하다. AI 응답 자체는 비결정적이므로 mock 응답으로 스냅샷을 잡는다.

**Contract testing (계약 검증)**: Forge 단계에서 정의된 API 계약(contracts.yml)이 실제 코드의 인터페이스와 일치하는지 검증한다. OpenAPI 스키마 검증 또는 타입 레벨 단언으로 한다. 계약과 구현이 어긋나면 빌드를 실패시킨다.

**Golden file testing (골든 파일 검증)**: CLI 출력처럼 결과가 사람이 읽는 텍스트일 때. 기대 출력을 별도 파일로 저장하고 실제 출력과 diff한다. 출력 형식 변경 시 의도적인 갱신이 일어나도록 강제한다.

**Manual review checklist (수동 검수)**: 자동화 못 하는 영역. Reality Check 리포트가 너무 비관적이지 않은지, 사용자 응답이 답을 강요하지 않는지, CLI 메시지가 비기술 창업자에게 통하는지. 이런 항목은 PR 리뷰어가 체크리스트로 본다. docs/manual-checklist.md에 정리한다.

### 어떤 패턴을 언제 쓰는가
- 도메인 로직의 정상 케이스: Given-When-Then
- 입력 조합이 많은 함수: Property-based
- 출력 구조 보호: Snapshot 또는 Golden file
- 단계 간 인터페이스: Contract
- 사용자 경험 정성 평가: Manual review

테스트가 동작 검증을 넘어 문서 역할을 하도록 쓴다. 테스트 이름만 읽어도 모듈이 무엇을 보장하는지 알 수 있어야 한다.

### 커버리지 정책
- core, catalog: 90% 라인 커버리지 이상. 이들이 곧 프로토콜이다
- cli, ai: 70% 이상. UX 코드는 단위 테스트가 어려우니 E2E에 기댄다
- 테스트 없는 새 코드는 머지되지 않는다. 시급성과 무관하다

### CI 강제 게이트
1. 린트 통과 (eslint + prettier)
2. 타입 체크 통과
3. 모든 테스트 통과
4. 생성된 .byeorim/ 디렉토리가 packages/catalog/schemas/의 JSON 스키마를 통과
5. ADR이 필요한 결정에 ADR이 빠져있지 않을 것

---

## 5. 역사 요구사항 (HISTORY — EVERY SESSION LEAVES A TRACE)

Byeorim은 코드 더미가 아니라 지식베이스가 되어야 한다.

### 흔적을 남기는 세 가지 방법
의미 있는 작업 세션마다 다음을 작성하거나 갱신한다.

1. **워크로그** (docs/worklog/YYYY-MM-DD.md): 무엇을 했고, 무엇을 배웠고, 무엇이 아직 열려있는지. 추가만 가능, 역사를 다시 쓰지 않는다
2. **ADR** (docs/decisions/NNNN-슬러그.md): 아키텍처 선택을 했다면 섹션 6 참조
3. **카탈로그 진화 노트** (docs/catalog-evolution/): 블럭 카탈로그나 의존성 그래프가 바뀌었을 때. 미래 기여자는 왜 `coupon → settlement`이 `requires`가 아니라 `affects`로 추가됐는지 추적할 수 있어야 한다

### 규칙
6개월 뒤 미래의 기여자(사람이든 AI든)가 이 저장소를 열고 "이건 왜 이렇지?"라고 묻는다면, 답이 이미 어딘가에 적혀있어야 한다.

### 워크로그 템플릿 (이 형식 그대로 사용)
```markdown
# Worklog — YYYY-MM-DD

## 이번 세션의 목표
한 문장으로. 무엇을 이루려 했는가.

## 변경 사항
- 손댄 파일
- 영향받은 모듈
- 추가/수정된 테스트

## 배운 점
당연하지 않았던 발견. 다음 사람이 이걸 적어둔 걸 고마워할 만한 것.

## 해야 할 일
다음 세션에서 꼭 풀어야 한다. 이번 세션이 약속하고 안 끝낸 후속 작업, 또는 결이 깨진 채로 남아있어 풀지 않으면 결함이 되는 것.

## 추천
다음에 이렇게 가면 좋겠다. 지금 안 해도 프로젝트가 안 멈추지만 했을 때 더 좋아지는 제안. 우선순위 의견을 한 줄 붙이면 다음 세션이 빠르게 결정한다.

## 알아둘 것
관찰. 미래 결정의 재료지만 지금 행동을 요구하지 않는 메모. "이런 사실이 있다"는 결.

## 링크
- ADR: docs/decisions/NNNN-슬러그.md
- PR / 커밋
```

세 갈래(해야 할 일, 추천, 알아둘 것)는 비어 있어도 머리만 남겨둔다. 다음 세션을 시작하는 사람은 "해야 할 일"부터 본 뒤 "추천"을 검토하고 "알아둘 것"은 흘려 읽어도 된다. 기존 "아직 열려있는 것" 한 묶음이 이 셋을 안 갈랐던 결을 풀어준다.

### 사용자 다이어리 vs 작업 워크로그
구분이 필요하다.
- **다이어리** (.byeorim/project/diary.md): 사용자가 답하지 못한 질문을 모아두는 곳. 사용자가 만들고 사용자가 본다
- **워크로그** (docs/worklog/): 기여자(개발자, AI)가 이 저장소에 한 작업을 기록하는 곳. 기여자가 만들고 기여자가 본다

둘은 다른 목적, 다른 위치다. 섞지 않는다.

---

## 6. ADR 작성 (ARCHITECTURE DECISION RECORDS)

벼림의 약속을 바꾸는 모든 결정에는 ADR이 필요하다. 7단계 메타포(ADR 0006으로 확장), .byeorim/ 디렉토리 형식, 의존성 타입처럼 외부에 노출된 동작이 여기 해당된다.

### ADR이 반드시 필요한 경우
- 새로운 최상위 개념 추가 (예: Inspect 다음 8번째 단계)
- .byeorim/ 디렉토리 레이아웃 변경
- requires/affects 외의 새 의존성 타입 추가
- 사소하지 않은 이유로 한 라이브러리를 다른 라이브러리보다 선택
- 7단계 메타포의 명령어 이름이나 별칭 변경
- Reality Check 6영역 변경

### ADR이 필요 없는 경우
- 버그 수정, 오타 수정
- 외부 동작에 영향 없는 내부 리팩토링

### ADR 템플릿 (docs/decisions/NNNN-슬러그.md)
```markdown
# ADR NNNN — 짧은 제목

- 상태: 제안 | 채택 | ADR-XXXX로 대체됨
- 날짜: YYYY-MM-DD
- 결정자: 이름 또는 역할

## 맥락
무슨 문제가 있는가. 어떤 힘들이 작용하고 있는가.

## 검토한 옵션
1. 옵션 A: 장점 / 단점
2. 옵션 B: 장점 / 단점
3. 옵션 C: 장점 / 단점

## 결정
무엇을 선택했고, 왜 그랬는지 한 문단으로.

## 결과
- 긍정적
- 부정적 / 트레이드오프
- 이 결정이 미래에 우리를 어디에 묶어두는가

## 링크
- 관련 이슈, 이전 ADR, 외부 참고자료
```

---

## 7. 작업 흐름 (HOW TO WORK)

### 코딩을 시작하기 전
1. 이번 세션에서 아직 안 읽었다면 docs/MANIFESTO.md 읽기
2. CLAUDE.md(이 문서) 읽기
3. 손댈 영역과 관련된 ADR 읽기
4. 해당 패키지의 기존 테스트 읽기. 의도를 주석보다 잘 설명한다
5. docs/worklog/ 가장 최근 항목 읽기. 마지막 사람이 무엇을 하고 있었는가

### 지시를 받았을 때
구체적이고 작은 지시(1 파일 이하, 50 라인 이하)는 바로 진행한다.

크거나 모호한 지시는 계획을 먼저 제안한다. 다음 형식으로.

```
목표: 한 문장
손댈 파일: 목록
추가할 테스트: 목록
ADR 필요?: 예/아니오 — 예라면 제목 초안
리스크 / 열린 질문: 목록
```

승인 후에 코드를 쓴다.

### 작업이 끝났을 때
1. 섹션 4의 검증 게이트 전체 실행
2. 워크로그 갱신
3. 해당하면 ADR 작성
4. 응답에서 요약: 무엇이 바뀌었고, 이제 무엇이 가능해졌고, 무엇이 아직 열려있는가

### 확신이 없을 때
물어라. 추측하지 마라. 잘못된 가정이 코드로 커밋되는 것은 질문하는 것보다 훨씬 나쁘다. "코드 전에 설계"가 전제인 프로젝트에서 이는 타협 불가다.

---

## 8. 코드 컨벤션 (CODE CONVENTIONS)

- **언어**: Node.js ESM, Node 18 이상
- **스타일**: prettier 기본값. 자전거 차고 색깔 논쟁 금지
- **주석**: 도메인 로직에는 한국어 주석 환영. 공개 API 문서와 ADR은 한국어로 작성한다(필요시 영문 마커 혼용)
- **네이밍**: 도메인 용어는 7단계 메타포에서 가져온다 (ADR 0006으로 6단계에서 7단계로 확장됐다)
  - 명령어: prospect, smelt, shape, forge, temper, set, inspect
  - 별칭: prs, sml, shp, frg, tmr, set, ins
  - 함수명도 메타포를 따른다. `extract()`보다 `smelt()`가 옳다
- **에러**: 절대 조용히 삼키지 마라. catch한다면 맥락과 함께 로그한다. 복구 불가하면 비개발자도 이해할 수 있는 메시지로 throw한다
- **보안**: 모든 YAML은 신뢰하기 전에 스키마로 검증한다. 악의적 카탈로그 파일이 코드를 실행하거나 샌드박스를 탈출할 수 없어야 한다
- **사용자 출력**: 모든 CLI 출력은 비기술 창업자가 이해할 수 있어야 한다. 영문 에러 스택을 그대로 보여주지 않는다. 한국어로 풀어쓴 메시지가 우선, 디버그 정보는 `--verbose` 플래그 뒤로 숨긴다
- **패턴 일관성**: 같은 종류의 일은 같은 패턴으로 짠다. 새 함수를 짜기 전에 같은 패키지의 기존 함수가 어떤 결로 짜여있는지 먼저 본다. 에러 처리 방식, 인자 순서, 반환 형태, 파일 헤더 주석 패턴을 따라간다. 의도적으로 다르게 짤 이유가 없으면 다르게 짜지 않는다. 6개월 뒤 기여자가 한 파일을 읽고 다른 파일에서 같은 패턴을 기대할 수 있어야 한다
- **파일당 책임**: 하나의 기능은 가능한 한 하나의 파일에 둔다. 한 파일이 두세 기능을 묶고 있으면 분리한다. 파일 단위 책임이 명확해야 다음 기여자가 어디를 고쳐야 하는지 빠르게 안다. 새 기능을 추가할 때 기존 파일에 끼워 넣기보다 새 파일을 만들 곳을 먼저 본다. 같은 패키지 안에서 기능 단위로 갈라둔다(예: src/load.js, src/validate.js, src/dependency.js)
- **데이터 무관성**: 코드 본문에 commerce 카탈로그의 특정 ID(coupon, payment, refund 등)나 도메인 가정을 박지 않는다. commerce는 벼림을 쓰는 방법을 보여주는 가이드 예시이지 표준이 아니다. 1순위 사용자(카페 사장님, 학원 원장님)가 commerce가 아닌 도메인 사람이라는 사실(섹션 0)을 코드도 지킨다. 카탈로그 형식만 가정하고 데이터 값은 가정하지 않는다. block.id나 dependency.type 같은 스키마 구조는 의지해도, "coupon이 결제에 영향을 준다" 같은 데이터 의미는 코드에 박지 않는다. 통합 테스트가 commerce를 예시로 쓰는 것은 괜찮다. 다만 그 단언이 데이터에 의존한다는 사실을 분명히 표시한다

---

## 9. 완료 정의 (DEFINITION OF DONE)

작업은 다음이 모두 참일 때 완료된다.

- [ ] 코드가 컴파일, 린트, 타입 체크를 통과한다
- [ ] 테스트가 작성되어 있고 통과한다 (단위 + 통합, 해당하면)
- [ ] 커버리지 임계치를 충족한다
- [ ] 생성된 .byeorim/ 산출물이 스키마 검증을 통과한다
- [ ] 섹션 6에 해당하면 ADR이 작성되어 있다
- [ ] 워크로그 항목이 추가되어 있다
- [ ] 절대 금지선(섹션 2)을 어기지 않았다
- [ ] 비기술 창업자가 결과 CLI 출력을 보고 무슨 일이 일어났는지 이해할 수 있다. 그러지 못한다면 UX가 미완성이다
- [ ] 모든 문서가 섹션 10의 글쓰기 원칙을 따른다
- [ ] 외부 진입점 문서(README, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY)를 수정했다면 한/영 두 판본을 같은 PR에서 갱신했다 (ADR 0056). 한국어판이 정본

---

## 10. 글쓰기 원칙 (WRITING RULES — MUST FOLLOW)

Byeorim의 모든 문서를 작성할 때 이 원칙을 적용한다. README, MANIFESTO, ADR, 워크로그, 코드 주석, 사용자 응답까지 전부 해당된다. 사람이 친구에게 설명하듯 자연스러운 한국어로 쓴다.

### 반드시 안 할 것 (MUST NOT)

다음 여덟 가지는 AI가 만든 글의 명백한 지문이다. 어떤 문서에도 쓰지 않는다.

**em dash (—)** 사용 금지. 한국어 글에서 em dash는 부자연스럽다. 대신 마침표나 쉼표로 끊는다. 정 길게 잇고 싶으면 쉼표로 호흡을 만든다.

**별표 강조** 본문 금지. *기울임*과 **굵게**를 본문 안에 흩뿌리지 않는다. 사람은 평문 한 단락 안에 강조를 거의 안 쓴다. 정말 강조해야 하면 그 한 단어가 들어간 짧은 문장 자체를 따로 분리한다.

**가짜 인용구** 금지. "이건 한 번씩 떠올려주세요"처럼 AI가 사용자에게 말하는 척 인용구를 넣지 않는다. 인용구는 진짜 누가 한 말을 옮길 때만 쓴다.

**화살표 (→) 남발** 금지. "X → Y → Z" 같은 도식을 산문에 박지 않는다. 흐름을 나타내고 싶으면 문장으로 풀어 쓴다. 표나 다이어그램이 정말 필요하면 그때 쓴다.

**광고 카피 같은 끝맺음** 금지. "그때 비로소 ~합니다", "이것이 ~의 본질입니다" 같은 시적 마무리는 한국어 문서에 어울리지 않는다. 마지막 문장도 본문과 같은 결로 자연스럽게 끝낸다.

**번역체 종결** 회피. "~할 수 있습니다", "~인 것입니다", "~하기 위함입니다" 같은 영어 직역체를 줄인다. 가능하면 단정형("~한다", "~이다")을 쓴다. 존댓말이 꼭 필요한 문서에서도 자연스러운 한국어 호흡을 잃지 않는다.

**과장된 형용사** 금지. "강력한", "탁월한", "혁신적인", "획기적인", "본질적인" 같은 단어는 마케팅 문서가 아니면 빼라. 구체적 사실이 더 강하다.

**"자리" 남발** 금지. "미래 자리", "검증 자리", "그 자리에서", "별도 ADR 자리"처럼 의미가 모호한 채로 "자리"를 채워 쓰지 않는다. 의미가 모호할 때는 구체 단어로 풀어 쓰거나 그냥 뺀다. 다음 표를 기본으로 쓴다.

| 기존 | 다듬은 결 |
|---|---|
| 미래 자리 | 다음 단계, 후속 작업, 차후 |
| 검증 자리 | 검증 항목, 검증 |
| 별도 ADR 자리 | 별도 ADR로 미룬다 |
| 그 자리에서 | 그 부분에서, 그 단계에서 |
| cleanup 자리 | 정리 작업 |
| ~ 자리 한 곳 | ~ 한 곳, ~ 한 부분 |
| ~ 자리에 박힌다 | ~에 박힌다 |

"자리"가 진짜 위치 또는 슬롯을 가리킬 때는 그대로 쓴다. 예를 들어 "스키마의 옵셔널 필드 자리"처럼 데이터 구조의 한 항목을 가리키는 결.

### 반드시 할 것 (MUST)

**마침표, 쉼표, 줄바꿈으로 호흡 정리**. 한 문장이 너무 길면 끊는다. 두 개념을 잇고 싶으면 쉼표 하나로 충분하다. em dash나 화살표를 부르지 않는다.

**짧은 문장과 긴 문장의 리듬**. 모든 문장이 짧으면 답답하고 모두 길면 지친다. 두세 문장 이어 쓴 뒤 한 문장으로 끊는다. 한국어가 자연스럽게 가지는 호흡이 있다.

**단정형 종결 적극 사용**. 기술 문서, ADR, 워크로그는 단정형으로 쓴다. 사용자 응답은 존댓말이지만 그 안에서도 핵심은 단정적으로 박는다.

**구체적 명사와 동사**. "성능을 개선합니다"보다 "쿼리 시간을 200ms에서 50ms로 줄였다". "확장성이 뛰어납니다"보다 "이 패턴은 모듈을 추가할 때 기존 코드를 안 건드린다".

**사용자가 누구인지 잊지 않는다**. Byeorim 문서를 읽는 사람은 비개발자 창업자거나 신사업 개발자다. 시니어 개발자만 아는 줄임말과 패턴 이름을 풀어 쓴다. 어쩔 수 없이 써야 하면 한 줄 설명을 붙인다.

### 점검 신호

다음 중 하나라도 해당되면 다시 쓴다.

- 첫 단락에 em dash가 두 개 이상 있다
- 한 단락 안에 별표 강조가 세 개 이상 있다
- 한 단락에 "자리"가 세 번 이상 나온다
- "비로소", "마침내", "그때" 같은 시적 부사로 단락이 끝난다
- 문장 끝이 다섯 줄 연속 같은 종결어미다
- "~을 통해", "~에 있어", "~함에 있어" 같은 번역체 조사가 두 번 이상 나온다
- 읽는 사람이 "AI가 만든 글 같다"고 말한다

마지막 신호가 가장 중요하다. 누군가 그 말을 하면 그 부분에서 멈추고 다시 쓴다.

### 예시: 같은 의미, 다른 결

AI 지문 결.
> Byeorim은 AI 시대의 *설계 강제 프로토콜*입니다. 사용자의 막연한 아이디어를 — 코드로 변환하기 전에 — 체계적으로 구조화합니다. **AI는 시니어 개발자처럼 묻고**, 사용자는 일상의 언어로 답합니다. 그 대화의 끝에서, 비로소 코드가 시작됩니다.

사람의 글 결.
> Byeorim은 AI로 자신의 서비스를 만들고 싶은 사람을 위한 협업 프로토콜이다. 막연한 아이디어를 곧장 코드로 바꾸기 전에, 그 서비스가 어떤 세계와 블럭으로 이루어질지 함께 본다. AI는 같이 회사를 키울 동료처럼 꼼꼼히 묻는다. 다만 답을 강요하지 않는다. 답하지 못한 질문은 다이어리에 남겨두고 함께 다음 단계로 간다.

두 글이 같은 내용을 담고 있다. 하나는 AI 지문이고 하나는 사람의 결이다. 차이를 보면 패턴이 보인다.

---

## 11. 원격 모드 견고함 (REMOTE-MODE ROBUSTNESS)

사용자가 원격 컨트롤러로 외부에서 작업을 맡길 때가 많다. 이때 사용자가 즉시 개입할 수 없으니 도구 호출 한 군데에서 흐름이 끊기면 작업이 멈춰버린다. 다음 결을 따라 견고하게 일한다.

### 도구 결과 손실에 자동 재시도

`Tool result missing due to internal error` 또는 비슷한 일시적 결과 누락이 보이면 사용자에게 묻지 않고 같은 호출을 한두 번 재시도한다. 인프라 일시 글리치라 같은 호출이 그대로 통과하는 경우가 많다.

다섯 번 연속 같은 호출에서 막히면 일시 글리치가 아니라 진짜 막힘이다. 그때 멈추고 현재 상태를 사용자에게 안내한 뒤 결정을 기다린다. 무한 재시도 루프는 만들지 않는다.

### 읽기 전용 명령은 한 호출로 묶기

상태 확인을 위한 git status/diff/log 같은 관련 읽기 명령은 한 Bash 호출에 `&&`로 묶어 보낸다. 결과가 한 곳에 모여 도구 시스템 일시 글리치에서 부분 손실 위험이 줄어든다.

```bash
# 권장
git status -u && git diff --stat && git log --oneline -5

# 비권장(병렬 셋이 동시 손실되면 모두 잃음)
[병렬] git status, git diff, git log
```

### 병렬 배치는 보수적으로

작업 의존성이 있으면 순차로 간다. 정말 독립적이라도 한 메시지에서 두세 개까지만 병렬에 둔다. 다섯 개 이상 병렬은 도구 결과 동시 손실 위험이 커진다.

### 위험 작업은 한 번 더 확인

원격 모드라도 destructive 작업(force push, 브랜치 삭제, rm -rf 등)은 평소처럼 사용자 확인을 받는다. 자동 진행하면 잘못해서 작업을 잃을 수 있다. 다만 read-only나 reversible 동작(파일 편집, 테스트 실행)은 자동 재시도 결을 그대로 따라간다.

### 자가 진단 출력

같은 호출에서 두 번 이상 막히면 짧게 자가 진단을 한 줄 보고한다. 무엇을 시도했고 어떤 결과였는지. 사용자가 원격에서 보고 결정하기 좋은 정보다.
