# 보안 정책 (Security Policy)

벼름은 무료 오픈소스 프로토타입이지만, 사용자 도메인의 카탈로그와 의존성을 다루기 때문에 보안 이슈는 진지하게 본다. 이 문서는 보안 결함을 발견했을 때 어떻게 알리고 어떻게 처리되는지 설명한다.

## 어떤 것이 보안 이슈인가

다음에 해당하면 일반 이슈가 아닌 보안 채널로 알린다.

- 임의 코드 실행으로 이어질 수 있는 결함 (예: 카탈로그 파일이 샌드박스를 탈출)
- 인증/권한 우회
- 사용자가 모르는 사이에 외부로 데이터를 유출하는 결함
- 의존성 패키지의 알려진 취약점이 벼름에 그대로 노출되는 경우
- 파일시스템 경로 탈출(path traversal)이나 임의 파일 읽기/쓰기

일반 버그(예: 명령이 의도와 다르게 동작, 잘못된 메시지 출력, 성능 문제)는 보안 이슈가 아니다. 일반 [GitHub Issues](https://github.com/jinikjang88/beoreum/issues)에 올린다.

## 어떻게 알리는가

[GitHub의 비공개 보안 신고(Private Security Advisory)](https://github.com/jinikjang88/beoreum/security/advisories/new)로 알린다.

이 채널은 메인테이너에게만 보이고 패치가 준비될 때까지 비공개로 유지된다. 공개 이슈로 보안 결함을 올리면 패치가 나오기 전에 다른 사용자가 그대로 영향받을 수 있다. 비공개 채널을 먼저 쓴다.

알려줄 때 다음을 포함하면 처리가 빠르다.

- 결함을 재현하는 최소 단계
- 영향 범위 (어떤 데이터, 어떤 명령에 영향이 있는지)
- 확인된 벼름 버전과 Node.js 버전
- 가능하다면 패치 제안

## 처리 약속

벼름은 1인 메인테이너가 운영하는 프로젝트라 24/7 대응은 약속하지 못한다. 다만 다음은 지킨다.

- 신고 접수 후 5영업일 안에 첫 응답
- 결함이 확인되면 영향 평가를 신고자에게 공유
- 패치 준비 중에도 신고자와 진행 상황을 공유
- 패치 공개 시점은 신고자와 함께 정한다 (조정된 공개, coordinated disclosure)
- 원하는 신고자는 릴리스 노트와 보안 권고문(Security Advisory)에 이름을 올린다. 원하지 않으면 익명 처리

## 지원 버전

현재 벼름은 0.x 단계다. 안정 API를 약속하기 전이라 하나의 활성 라인만 유지한다.

| 버전 | 보안 패치 |
|------|----------|
| 최신 0.x | 지원 |
| 그 이전 | 지원하지 않음 |

1.0이 나오면 이 표를 갱신한다.

## 안전한 사용 권고

벼름을 직접 사용하는 동안 다음을 지키면 결함의 영향이 줄어든다.

- 신뢰할 수 없는 출처의 카탈로그 YAML을 그대로 받지 않는다. 검토 없이 임포트하면 의존성 그래프나 블럭 정의가 오염될 수 있다
- `.beoreum/` 디렉토리를 외부에 공유할 때는 `diary.md` 같은 사용자 메모가 들어있을 수 있다는 점을 본다
- Node.js는 [공식 LTS](https://nodejs.org/en/about/previous-releases) 라인을 쓴다. EOL 버전은 지원하지 않는다
