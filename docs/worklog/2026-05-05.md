# Worklog — 2026-05-05

## 이번 세션의 목표

prospect가 세 PR로 모두 벼려진 뒤 다음 자리인 smelt를 벼린다. 비기술 창업자(1순위 사용자)가 picker에서 막막함 없이 자기 도메인에 어울리는 블럭을 고르고, 자동 추가된 자리를 한 번 보고 결정할 수 있게 한다. ADR 0029(블럭 추천 + 선택 검토)로 한 묶음.

## 변경 사항

### 새 파일
- docs/decisions/0029-smelt-block-recommendation-and-review.md — 결정 4개(추천 메서드 자리, picker 강조 방식, 검토 단계 강제력, adapter 미구현 시 동작)
- docs/specs/block-recommendation.md — recommendBlocks 약속, 추천 기준, mock 결정적 휴리스틱, claude 약속

### 손댄 파일
- packages/ai/src/adapter.js — BlockRecommendation typedef + AiAdapter에 recommendBlocks 시그니처(선택 메서드)
- packages/ai/src/mock.js — recommendBlocks 결정적 구현(priority='required' 우선 + 부족분 catalog 순서로 5개 + 10개 상한)
- packages/ai/src/claude.js — RECOMMEND_SYSTEM_PROMPT 한국어 본문, RECOMMEND_OUTPUT_SCHEMA, max_tokens 2048, recommendBlocks 호출 시 catalog.blocks만 잘라 보내 토큰 절약. catalog에 없는 ID는 어댑터에서 안전망으로 필터
- packages/cli/src/smelt.js — readIntent 헬퍼(schema_version 1/2 폴백), fetchRecommendation 함수, runSmelt는 시그니처 그대로 유지(비대화 호환), interactiveSmelt에 adapter+confirmSelection 의존성 주입, picker → resolveAll → confirmSelection 루프
- bin/beoreum.js — smelt 명령에 selectAdapter() 결과 전달
- docs/decisions/0009-ai-adapter-interface.md — 머리에 ADR 0029의 recommendBlocks 추가 표기 + ADR 0027의 generateRealityCheck 표기도 보충
- README.md — 7단계 표의 smelt 한 줄 갱신, "제련 한 번 돌려보기" 새 절 추가

### 추가/수정된 테스트
- tests/unit/cli-smelt.test.js — 기존 picker 시그니처(catalog 단독)를 새 시그니처({catalog, recommendation})로 갱신, 추천/검토 흐름 6개 시나리오 추가(picker가 recommendation 받음, adapter 미구현 시 graceful, redo 흐름, confirmSelection 인자 검증, schema_version 1 폴백)
- tests/unit/ai-mock.test.js — recommendBlocks 5개 시나리오(priority 우선 + 5개 채움, 결정성, 빈 catalog, 인자 없이, 10개 상한)
- tests/unit/ai-claude.test.js — claude 어댑터가 다섯 메서드 가지는지 확인, recommendBlocks 5개 시나리오(parsed_output 통과 + 안전망, max_tokens 2048, compact catalog 전달, schema required, parsed_output 폴백)

### 검증 결과
- npm test 320개 모두 통과(이전 305 + SM 신규 15)
- ESLint, Prettier 통과

## 배운 점

picker 시그니처를 바꾸면서 기존 헬퍼 테스트들이 깨졌다. `pickBlocks(catalog)` 단일 인자에서 `pickBlocks({ catalog, recommendation })` 객체 인자로. 의존성 주입 패턴을 쓸 때 인자 모양을 처음부터 객체로 두면 미래 확장 시 호환성이 살아남는다. 이 자리에서 한 번 학습.

claude 어댑터의 recommendBlocks는 catalog 전체를 보내지 않고 blocks의 일부 필드(id, name, user_desc, priority)만 잘라 보낸다. catalog가 큰 경우 토큰 비용이 빠르게 늘 수 있어서 어댑터 단계에서 자르는 결이 안전. 결정 1(adapter에 새 메서드)을 채택한 다른 이유 자리이기도 하다 — 어댑터가 입력을 자기 입맛에 맞게 정리할 자리를 가짐.

confirmSelection을 'redo'/'proceed' 두 갈래로 한 결이 흐름을 단순하게 유지했다. 셋(예/아니오/자세히)으로 가면 "자세히"가 무엇을 의미하는지 사용자가 헷갈리고, 차단 없이 보여주기만 하면 사용자가 안 읽고 넘어간다. 두 갈래 select가 동행 톤(읽고 결정)과 흐름 가벼움 사이의 균형점.

intent.yml schema_version 1 폴백 자리가 후속 하위 호환의 좋은 예. ADR 0026이 1→2를 박았을 때 마이그레이션 안 둔다 결정했지만, 실제로는 옛 파일을 만나면 빈 user_answers로 폴백해 추천이 빈 배열로 나오게 graceful degradation. 사용자에게 안내 한 줄("AI 추천 없이 진행")로 알린다.

## 아직 열려있는 것

- claude 어댑터의 recommendBlocks를 실제 사용자 도메인으로 manual review 자리. 카페 사장님 1~2명에게 추천이 의미 있는지 확인 필요
- recommendBlocks가 추천 점수(0~1)를 노출해 강한 추천과 약한 추천을 구분하는 자리. 후속 ADR
- "왜 이 블럭이 추천 안 됐나?" 묻는 reverse 추천. 후속 자리
- prospect 끝에서 smelt 인터랙티브로 자동 이어지는 자리(예: `--continue` 플래그). 지금은 사용자가 두 명령을 따로 친다
- cascade 질문(decisions.yml)을 smelt 끝에서 바로 묻는 자리는 안 건드림. answer 명령이 별도. 후속 자리

## 링크
- ADR: docs/decisions/0029-smelt-block-recommendation-and-review.md
- 명세: docs/specs/block-recommendation.md
- 갱신된 ADR: docs/decisions/0009-ai-adapter-interface.md
- 짝 ADR: 0010(smelt 산출물 그대로), 0011(picker 의존성 주입 결 확장), 0026(intent.yml schema_version 2 의존)
