# Worklog — 2026-05-07

## 이번 세션의 목표

CLAUDE.md의 글쓰기 결을 다듬고(자리 과용 금지, 워크로그 템플릿 갱신), set이 만든 코드가 로컬에서 한 번에 떠서 살아있는 것을 보여주도록 baseline을 박는다(ADR 0046, PR 1).

## 변경 사항

### 새 파일

- docs/decisions/0046-generated-runnable-baseline.md — 6개 결정(healthcheck 표준 emit, .env/.env.production 둘 다, in-memory dev DB, 두 안전망(파일 분리 + 런타임 throw), `BEOREUM_DEV_PLACEHOLDER_` prefix, 4 generator 표준 idiom 정렬)

### 손댄 파일

- CLAUDE.md 섹션 10 — 반드시 안 할 것에 "자리 남발" 추가, 대체 단어 표 박음, 점검 신호에 한 줄 추가, 섹션 11의 자리 과용도 같이 정리
- CLAUDE.md 섹션 5 — 워크로그 템플릿을 "아직 열려있는 것" 한 묶음에서 "해야 할 일 / 추천 / 알아둘 것" 셋으로 갈라 갱신
- packages/cli/src/generators/util.js — DEV_PLACEHOLDER_PREFIX 상수와 devPlaceholder(keyName) 헬퍼 추가
- packages/cli/src/generators/node.js — buildEnvDev/buildEnvProduction/buildGitignore 추가, server.js에 dotenv 분기 로드 + /health endpoint + ensureProdSecrets 가드, package.json에 dev/start:prod 스크립트, README 갱신, .env/.env.production은 existsSync로 customization 보호
- packages/cli/src/generators/java.js — buildApplicationYml/buildApplicationProductionYml/buildHealthControllerJava/buildEnvSecretsValidatorJava/buildGitignore 추가, generateJavaBackend 본체에 새 파일 emit, README 갱신
- packages/cli/src/generators/python.js — buildEnvDev/buildEnvProduction/buildGitignore 추가, main.py에 /health endpoint, config.py에 PYTHON_ENV 분기 로드 + model_validator로 prod 가드, README 갱신
- packages/cli/src/generators/frontend.js — buildEnvDev/buildEnvProduction/buildGitignore 추가, vite.config.ts에 prod build 가드(loadEnv로 placeholder 검출 throw), README 갱신

### 추가/수정된 테스트

- tests/unit/generators-util.test.js — devPlaceholder + DEV_PLACEHOLDER_PREFIX 5개 시나리오 추가
- tests/unit/cli-set-node.test.js — ADR 0046 7개 시나리오 추가(/health, .env/.env.production, .gitignore, prod 가드, NODE_ENV 분기, scripts, customization 보호)
- tests/unit/cli-set-java.test.js — ADR 0046 5개 시나리오 추가(HealthController, application.yml/application-production.yml, EnvSecretsValidator, .gitignore, customization 보호)
- tests/unit/cli-set-python.test.js — ADR 0046 5개 시나리오 추가(/health, .env/.env.production, config.py 분기+가드, .gitignore, customization 보호)
- tests/unit/cli-set-frontend.test.js — ADR 0046 4개 시나리오 추가(.env/.env.production, vite.config 가드, .gitignore, customization 보호)

### 검증 결과

- npm test 586개 모두 통과(이전 547 + 신규 39)
- ESLint 0 에러
- Prettier 통과(generators 4개 + 테스트 4개 + ADR 0046)

## 배운 점

워크로그 8개를 다시 읽어보니 한 단락에 "자리"가 다섯 번 이상 박힌 곳이 여럿 있었다. AI 지문의 새 자국이 됐고, 의미가 모호한 채로 채우는 단어가 됐다. 사용자가 "추천 / 해야 할 일 / 이런 것들이 있다"로 갈라보자는 결을 줘서 셋으로 박았다. 다음 세션을 시작하는 사람이 "해야 할 일"부터 보고, "추천"을 검토하고, "알아둘 것"은 흘려 읽는 결로 분리. 단순한 분리지만 인지 비용이 줄어든다.

ADR 0046이 한 ADR로 6개 결정을 묶었지만 각 결정이 평행해서 한 묶음으로 풀렸다. healthcheck(결정 1)는 표준이라 4 generator가 각자 emit, 두 .env(결정 2)도 4 generator가 각자 idiom대로 만들고, 두 안전망(결정 4)도 4 곳에서 같은 placeholder prefix를 검사. 결정 6의 "각 언어 표준 idiom" 결을 박은 게 의미 있었다. Spring은 application.yml + profile, Node/Python은 .env, Vite는 mode. 각 언어 사용자가 자기 표준을 그대로 본다.

devPlaceholder 헬퍼 한 곳에 prefix 마커가 모여 있어 미래에 마커를 바꾸려면 그 한 곳만 갱신하면 된다. ADR 0046 결정 5의 표준 마커가 코드 한 곳에 박힌 결.

`existsSync`로 .env / .env.production / application.yml을 보호하는 결을 박았다. set이 다시 실행될 때 사용자가 채운 prod 값이 덮이지 않게. 다른 generated 파일(server.js, routes.js)은 항상 덮어쓰는데 env 파일만 customization-aware. 책임 분리: 코드는 generator의 결, env는 사용자의 결.

Java의 .gitignore는 application.yml은 commit 가능(dev placeholder만 박혀있어 안전), application-production.yml만 gitignore. Spring 관례를 따른 결이라 Java 사용자에게 자연스럽다. Node/Python은 .env 둘 다 gitignore(이쪽 idiom). 4 generator가 같은 결을 각 언어 표준으로 풀어낸 결.

vite.config의 prod build 가드는 빌드 타임 검사라 runtime throw 결과 다르다. import.meta.env가 빌드 산출물에 박히기 때문에 빌드 자체에서 막아야 dev placeholder가 prod 번들에 안 들어간다. defineConfig 함수형 API + loadEnv 패턴이 깔끔하게 풀어줬다.

Python의 model_validator(mode="after")가 settings 인스턴스 검증의 자연스러운 결. pydantic v2의 데코레이터가 placeholder prefix 검사를 한 곳에 모은다. ValueError를 raise하면 pydantic이 ValidationError로 감싸 서버 시작 실패가 분명한 결로 사용자에게 도달.

ADR 0044의 singleton 결과 ADR 0046이 평행하게 살아남았다. /me 같은 singleton path가 set 단계에서 그대로 emit되고 .env/.env.production이 같이 자동 생성. 4 generator의 표준 idiom이 다르면서 baseline 의미는 같은 결로 박힌 결.

테스트 결정성이 살아남았다. 4 generator 단위 테스트가 LLM 호출 없이 끝까지 돌고, mock runCommand 같은 외부 프로세스도 없다. .env / application.yml 파일이 디스크에 만들어지는 결만 검증. CLAUDE.md 섹션 4의 5초 미만 단위 테스트 결.

## 해야 할 일

- ADR 0047(beoreum run 명령): backend + frontend 동시 기동 long-running 명령. healthcheck readiness 안내. ADR 0046 baseline이 박혔으니 다음 PR로 진행
- ADR 0048(verify 스모크 테스트 추가): server up → /health → kill 결을 verify에 박음. CI에서 "내 서비스가 진짜 뜨는가?"를 한 번에 검증
- shape.js/smelt.js의 unused eslint-disable 두 곳은 여전히 정리 작업
- stage-helpers.js 추출(공통 advanceState/loadState/ensureFile 결을 각 단계가 따로 박고 있음)도 정리 작업
- Java generator에 JPA 의존성과 H2 dependency를 넣어야 application.yml의 dev DB 설정이 실제로 동작. ADR 0046은 in-memory를 yml에만 박았고 의존성은 안 넣음. Java backend가 DB를 쓰는 시점에 한 묶음으로 풀어야 함

## 추천

- ADR 0047부터 가는 결이 자연스럽다. baseline이 막 박혀서 run 명령이 의지할 결이 분명하고, 사용자가 원한 "한 번에 띄움"의 핵심이 run 명령에 있다. verify 스모크는 그 다음 PR
- 미래에 docker-compose 부산물 ADR을 검토. 지금은 in-memory/SQLite로 외부 의존성을 0으로 만들었지만, 사용자가 PostgreSQL 같은 진짜 DB를 prod-like하게 dev에서 쓰고 싶으면 docker-compose가 결을 풀어줌. 다만 Docker 부담은 1순위 사용자에게 큰 문턱이라 옵션으로
- placeholder prefix 마커 검사를 모든 환경 변수에 자동으로 거는 결을 검토. 지금은 명시적으로 REQUIRED_SECRETS 배열을 박았는데, 미래에 사용자가 추가한 시크릿이 빠질 수 있음. settings의 모든 string 필드를 자동 검사하는 결로 갈 수도 있다(별도 ADR)

## 알아둘 것

- generator 본체의 fileCount 수가 새 파일들(.gitignore, .env, .env.production, HealthController, EnvSecretsValidator)로 늘었다. 테스트 단언 중 `>=` 임계값에 의지하던 부분은 그대로 통과
- placeholder prefix `BEOREUM_DEV_PLACEHOLDER_`는 23자라 짧진 않지만 검색 가능성과 의도 분명함이 우선이라 그대로. 다른 generator에서 검사 로직이 startsWith 한 줄이라 길이 부담은 작음
- frontend의 vite.config 가드가 빌드 시점에만 동작. dev 서버에서는 placeholder가 그대로 살아있어도 동작. 의도된 결(dev에서 작동해야 의미가 있음)
- Java의 H2 in-memory 설정은 application.yml에 박혔지만 build.gradle.kts에는 H2 의존성이 없다. 사용자가 JPA/DB 코드를 채울 때 같이 추가하는 결로 미룸. ADR 0046 결정 3의 in-memory 약속은 yml 한 곳에 박혔고 실제 동작은 미래 의존성 추가 시점에 살아남는다
- existsSync 보호가 처음 set 실행 시에는 무관(파일이 없어서 항상 만들어짐). 두 번째 실행부터 의미 있는 결. customization 보호 시나리오 테스트가 두 번 실행 결을 검증

## 링크

- ADR: docs/decisions/0046-generated-runnable-baseline.md
- 갱신 대상: ADR 0018/0019/0020/0021 (4 generator의 baseline 보강), ADR 0022 (verify 결과 짝)
- 후속 작업: ADR 0047(beoreum run), ADR 0048(verify 스모크 테스트)
- 입력 의존: ADR 0017 결정 1(보안 최우선), ADR 0044(singleton)
- 동행 톤: ADR 0003 결정 2
- 관련 정책: CLAUDE.md 섹션 0(1순위 사용자), 섹션 4(테스트 결정성), 섹션 8(파일당 책임, 데이터 무관성), 섹션 10(글쓰기 원칙, 자리 과용 금지)
